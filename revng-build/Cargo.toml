[package]
name = "revng-build"
version = "0.1.0"
edition = "2024"
description = "Build-script support for locating and linking a staged revng SDK and its pinned LLVM"
license = "MIT"

[dependencies]
cc = "1"
fd-lock = "4"
flate2 = "1"
pkg-config = "0.3"
tar = "0.4"
thiserror = "2"
ureq = "3"
